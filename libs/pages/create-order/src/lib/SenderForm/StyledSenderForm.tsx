import styled from 'styled-components';
import { StyledForm } from '../StyledForm';

export const StyledSenderForm = styled(StyledForm)`
  .button-container {
    .p-button {
      margin: 0 16px;
    }
  }
`;
